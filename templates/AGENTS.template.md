# <项目名> — Agent 准则

> 复制本模板到新项目根目录并改名为 `AGENTS.md`，替换所有 `<占位符>`（含文档链接路径，复制后按实际位置修正相对链接），按项目实际修改。用户明确指令优先于本文档。

## 1. 项目概览

<一句话描述项目>

<当前阶段 / 目标；如有路线图文档则附链接>

## 2. 技术栈与约束

- <语言 / 框架 / 版本>
- <关键依赖或外部服务>
- <运行、部署或性能约束>

## 3. 常用命令

| 目的 | 命令 |
|------|------|
| 安装依赖 | `<命令>` |
| 构建 | `<命令>` |
| 测试 | `<命令>` |
| 格式化 | `<命令>` |
| 静态检查 | `<命令>` |
| 启动开发服务 | `<命令>` |

> 拿不准用什么命令时，参考 `<Git 工作流文档>` 第 5 节的各语言对照表。

## 4. 目录结构

| 路径 | 说明 |
|------|------|
| `<src/>` | <说明> |
| `<docs/>` | 设计、实现、接口文档 |
| `<配置目录/>` | <说明> |

## 5. 编码规范

- <命名 / 风格约定>
- <错误处理约定>
- <日志约定>
- 新增 / 修改公共函数必须补测试；
- 行为变更必须同步更新文档（见 `<文档通信设计文档>`）。

## 6. 安全红线

- 密钥、凭据、token 不得写入日志、文档、提交内容或 HTTP 响应；
- <敏感目录 / 文件清单，如 .env、密钥库>
- <其他必须遵守的安全约束>

## 7. 核心流程

<业务关键流程，按项目实际填写，例如请求处理顺序、数据流、权限模型>

## 8. Git 工作流

- 新分支使用 `<Agent 工具名/角色/身份>/` 前缀；
- 提交前必须通过：格式化、静态检查、测试、构建；
- 提交信息使用 Conventional Commits（`feat:` / `fix:` / `docs:` / `refactor:` / `test:` / `chore:`）；
- 个人操作步骤见 `<个人 Git 工作流文档>`；
- 多人 / 多 Agent 协作规则见 `<协作工作流文档>`；
- 不提交与任务无关的改动；工作区已有未提交改动时，先确认归属再动手。
