# Agent 准则（本仓库）

本文件约束在 agent-workflows 仓库内工作的 Agent（如 Codex）与协作者。

## 目标

维护一套**语言无关、可直接复制到新项目**的多人 / 多 Agent 协作模板。

## 基本规则

- `templates/` 只放通用内容：不得出现具体项目名、语言专属命令（命令对照表除外）、仓库路径；
- 项目特有内容放 `examples/`；
- 改动模板必须同步更新 `README.md` 中的说明（文档同步）；
- 提交前检查：Markdown 链接有效、占位符格式一致（`<...>`）、无密钥；
- Git 流程：新分支使用 `codex/` 前缀；提交信息用 Conventional Commits；合并默认 squash；
- 本仓库内容会被其他项目复制，模板改动影响面大，因此必须经过 review 后再合并；
- 用户明确指令优先于本文档。
